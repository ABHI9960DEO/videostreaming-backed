# ?? MySQL (Dev) ??????????????????????????????????????????????
spring.datasource.url=jdbc:mysql://localhost:3306/videostream_db
spring.datasource.username=root
spring.datasource.password=root@admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? Hibernate DDL (dev only ? auto-creates tables) ???????????
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ?? File Storage (local) ?????????????????????????????????????
app.storage.type=local
app.storage.local.base-path=/tmp/videostream/uploads

# ?? JWT ??????????????????????????????????????????????????????
app.jwt.secret=dev_super_secret_key_change_in_prod_min_32_chars
app.jwt.access-token-expiry-ms=900000        # 15 minutes
app.jwt.refresh-token-expiry-ms=604800000    # 7 days

# ?? CORS ?????????????????????????????????????????????????????
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000